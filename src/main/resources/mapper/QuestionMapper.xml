<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.virlearning.dao.QuestionMapper">
    <insert id="addQuestion" parameterType="com.example.virlearning.model.param.QuestionAddParam">
        insert into question(
            description,
            option_a,
            option_b,
            option_c,
            option_d,
            answer
        )values (
            #{description},
            #{optionA},
            #{optionB},
            #{optionC},
            #{optionD},
            #{answer}
        )
    </insert>

    <delete id="deleteQuestionById">
        update question set is_deleted=1 where is_deleted = #{questionId}
    </delete>
</mapper>